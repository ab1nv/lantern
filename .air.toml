# .air.toml
root = "."
tmp_dir = "tmp"
[build]
  cmd = "go build -o tmp/bin/lantern ./cmd/lantern/lantern.go"
  bin = "tmp/bin/lantern"
  include_ext = ["go"]
  exclude_dir = ["tmp", "node_modules"]
  delay = 100
  stop_on_error = false

[misc]
  clean_on_exit = true
